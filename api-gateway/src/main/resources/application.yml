
spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: provision-service
          uri: http://localhost:8080
          predicates:
            - Path=/provision/**

server:
  port: 8081
