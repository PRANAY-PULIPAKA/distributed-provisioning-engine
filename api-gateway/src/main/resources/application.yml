spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:

        - id: provision-service
          uri: http://localhost:8080
          predicates:
            - Path=/provision/**

        - id: auth-service
          uri: http://localhost:8080
          predicates:
            - Path=/auth/**

server:
  port: 8081